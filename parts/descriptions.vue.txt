// main.ts — register the element + mark aha-* as custom elements
import '@ahaslides-product/design/aha-descriptions';
app.config.compilerOptions.isCustomElement = (tag) => tag.startsWith('aha-');

// Component.vue
<script setup>
import { ref } from 'vue';
const rows = ref([['Plan', 'Pro'], ['Seats', '25 of 50'], ['Billing cycle', 'Yearly']]);
</script>

<template>
  <aha-descriptions desc-title="Account">
    <div v-for="[label, value] in rows" :key="label" :label="label">{{ value }}</div>
  </aha-descriptions>
</template>
