{
  "name": "InputNumber",
  "slug": "input-number",
  "group": "Data Entry",
  "tier": "composite-antd",
  "badge": "composite — antd wrappers",
  "summary": "Enter a number with steppers, min/max and precision — quantities, limits, durations — rendered through the shared InputNumber.",
  "lead": "InputNumber carries steppers, keyboard step/precision and a formatter/parser — more than a zero-dep web component should own, so it stays on real Ant: antd v6 (React) and ant-design-vue v4 (Vue), both themed once by the shared inputNumberTheme. Two implementations, one DS V3 look. This is the composite half of the hybrid.",
  "tokensUsed": ["colorPrimary", "borderRadius", "controlHeight", "colorBorder", "InputNumber.activeBorderColor", "InputNumber.hoverBorderColor"],
  "spec": [
    { "label": "Field", "value": "height 32 · radius 8 · 1px #E3E3E3 border" },
    { "label": "Hover", "value": "#D3B4FF (purple-30) border" },
    { "label": "Focus", "value": "brand #6A1EBB border" },
    { "label": "Steppers", "value": "up/down on hover; keyboard ↑ ↓" },
    { "label": "Placeholder", "value": "#8A8A8A" }
  ],
  "props": [
    { "name": "value", "type": "number", "default": "—", "desc": "Controlled numeric value" },
    { "name": "min", "type": "number", "default": "-Infinity", "desc": "Lower bound" },
    { "name": "max", "type": "number", "default": "Infinity", "desc": "Upper bound" },
    { "name": "step", "type": "number", "default": "1", "desc": "Increment for steppers / arrow keys" },
    { "name": "precision", "type": "number", "default": "—", "desc": "Fixed decimal places" },
    { "name": "onChange", "type": "(value) => void", "default": "—", "desc": "Fires with the new number" }
  ],
  "opinion": {
    "whenToUse": [
      { "what": "InputNumber", "when": "a bounded numeric quantity — a limit, a count, a duration where steppers help" },
      { "what": "Slider", "when": "an approximate value across a known range where the exact number matters less" },
      { "what": "Input type=number", "when": "a free number with no bounds or steppers — but prefer InputNumber for validity" }
    ],
    "note": "Set min/max to the real limits so the field constrains instead of validating after the fact. Pre-fill a sensible default rather than empty; use precision for currency/percentages."
  },
  "surfaces": ["editor", "dashboard", "settings"],
  "reuse": { "entry": "./input-number-theme", "exportsNamed": ["inputNumberTheme"] },
  "snippets": [
    { "key": "html", "label": "HTML", "file": "input-number.html.txt", "runnable": "paste-and-run", "note": "Composite has no framework-free element, so its paste-and-run form is a CDN-React page — React + antd load from a CDN and mount on open, consuming the same shared inputNumberTheme. No build step." },
    { "key": "react", "label": "React", "file": "input-number.react.txt" },
    { "key": "vue", "label": "Vue 3", "file": "input-number.vue.txt" },
    { "key": "theme", "label": "Theme (shared)", "file": "input-number.theme.txt" }
  ],
  "codeNote": "React and Vue are two real vendor libraries at two Ant generations — they can't share a primitive. They converge via the same token/theme (inputNumberTheme); residual drift is caught by visual regression.",
  "preview": "input-number.preview.html",
  "selfCheck": [
    { "label": "field 32 · radius 8 · #E3E3E3 border", "verdict": "PASS" },
    { "label": "hover border purple-30", "verdict": "PASS" },
    { "label": "focus border brand", "verdict": "PASS" },
    { "label": "placeholder #8A8A8A", "verdict": "PASS" }
  ],
  "conformance": {
    "$about": "Measured by qa.mjs against the RENDERED UI (getComputedStyle via headless CDP). Composite shown as a CDN-React page mounted at #react-root; the measure reads the real antd v6 InputNumber bound to inputNumberTheme.",
    "ready": "!!document.querySelector('#react-root .ant-input-number')",
    "measure": "(function(){var g=function(sel,p){var el=document.querySelector('#react-root '+sel);return el?getComputedStyle(el)[p]:null;};return{radius:g('.ant-input-number','borderTopLeftRadius'),radiusBR:g('.ant-input-number','borderBottomRightRadius'),border:g('.ant-input-number','borderTopColor'),borderWidth:g('.ant-input-number','borderTopWidth'),borderWidthB:g('.ant-input-number','borderBottomWidth'),height:g('.ant-input-number','height')};})()",
    "expect": { "radius": "8px", "radiusBR": "8px", "border": "rgb(227, 227, 227)", "borderWidth": "1px", "borderWidthB": "1px", "height": "32px" }
  }
}
