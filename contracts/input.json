{
  "name": "Input",
  "slug": "input",
  "group": "Data Entry",
  "tier": "leaf-lit",
  "badge": "shared Lit web component",
  "element": "aha-input",
  "summary": "A single-line text field — names, search, numbers, short free text.",
  "lead": "ONE custom element — <aha-input> — defined once, imported unchanged by React and Vue. Shadow-DOM CSS, themed only by --aha-* tokens, so every field reads identically across apps. State and type are attributes; it emits composed `input` / `change` events. Zero drift — the same element, not two look-alikes.",
  "codeNote": "Same element, same shadow-DOM CSS → identical render. React (< 19) consumes it via a thin ref wrapper; Vue binds the custom element natively. The primitive is defined once in @ahaslides-product/design.",
  "tokensUsed": ["border", "border-hover", "color-primary", "bg-container", "bg-container-disabled", "text-default", "text-tertiary", "radius-default", "focus-ring-soft", "border-error"],
  "spec": [
    { "label": "Box", "value": "height 32 (md) · radius 8 · padding 0 12" },
    { "label": "Sizes", "value": "sm 24 · md 32 · lg 40 — all radius 8" },
    { "label": "Rest border", "value": "1px #E3E3E3" },
    { "label": "Hover border", "value": "#D3B4FF (purple-30)" },
    { "label": "Focus", "value": "#6A1EBB border + 2px #D3B4FF@30% ring" },
    { "label": "Invalid", "value": "#F5222D border + red ring" },
    { "label": "Text", "value": "Plus Jakarta 14/21; placeholder #8A8A8A" }
  ],
  "props": [
    { "name": "value", "type": "string", "default": "\"\"", "desc": "Controlled text value" },
    { "name": "placeholder", "type": "string", "default": "—", "desc": "Hint shown when empty" },
    { "name": "type", "type": "text | number | email | password | search", "default": "text", "desc": "Native input type" },
    { "name": "size", "type": "sm | md | lg", "default": "md", "desc": "Control height — sm 24 · md 32 · lg 40 (DS V3 field scale); radius stays 8" },
    { "name": "disabled", "type": "boolean", "default": "false", "desc": "Non-interactive; grey fill" },
    { "name": "invalid", "type": "boolean", "default": "false", "desc": "Error styling (red border)" },
    { "name": "input", "type": "CustomEvent<{value}>", "default": "—", "desc": "Composed event on each keystroke; read e.detail.value" }
  ],
  "opinion": {
    "whenToUse": [
      { "what": "Input", "when": "short single-line free text — a name, a search term, a number" },
      { "what": "Textarea", "when": "multi-line free text (descriptions, messages)" },
      { "what": "Select", "when": "one value from a known set — don't make the user type it" }
    ],
    "note": "Label the field with a noun phrase; use placeholder for an example, never as the label. Show the invalid state only after the user leaves the field, and pair it with a message that says how to fix it.",
    "dsv3": "Sizes match the measured DS V3 input cells (sm/md/lg = 24/32/40, radius 8, default md 32 = controlHeight root). Rest border is bound to the `border` token (#E3E3E3): the measured cell reads #D4D4D4, but the reconciled token layer resolves the field border to the export brand gray #E3E3E3 (precedence EXPORT for brand colour), kept consistent across the whole field family (input/select/form/datepicker)."
  },
  "surfaces": ["editor", "dashboard", "settings", "audience"],
  "reuse": { "entry": "./aha-input", "registers": "aha-input", "exportsNamed": ["AhaInput", "defineAhaInput"] },
  "snippets": [
    { "key": "html", "label": "HTML", "file": "input.html.txt", "runnable": "paste-and-run", "note": "The native form — a web component in an HTML file renders on open, no build step. The fast path for vibe-coding." },
    { "key": "react", "label": "React", "file": "input.react.txt" },
    { "key": "vue", "label": "Vue 3", "file": "input.vue.txt" }
  ],
  "preview": "input.preview.html",
  "selfCheck": [
    { "label": "height 32 (md) · sm 24 · lg 40 · radius 8", "verdict": "PASS" },
    { "label": "rest border #E3E3E3", "verdict": "PASS" },
    { "label": "focus #6A1EBB border + ring", "verdict": "PASS" },
    { "label": "placeholder #8A8A8A 14/21", "verdict": "PASS" }
  ],
  "conformance": {
    "$about": "Measured by qa.mjs against the RENDERED UI (getComputedStyle via headless CDP). Leaf = ONE shared element, single render.",
    "ready": "!!(document.querySelector('aha-input[data-probe]') && document.querySelector('aha-input[data-probe]').shadowRoot && document.querySelector('aha-input[data-probe]').shadowRoot.querySelector('.field'))",
    "singleUI": true,
    "measure": "(function(){var el=document.querySelector('aha-input[data-probe]');var f=el&&el.shadowRoot&&el.shadowRoot.querySelector('.field');var s=f?getComputedStyle(f):null;return{height:s?s.height:null,radius:s?s.borderTopLeftRadius:null,border:s?s.borderTopColor:null,fontSize:s?s.fontSize:null};})()",
    "expect": { "height": "32px", "radius": "8px", "border": "rgb(227, 227, 227)", "fontSize": "14px" }
  }
}
