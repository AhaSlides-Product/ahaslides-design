{
  "name": "Info box",
  "slug": "info-box",
  "group": "Settings",
  "tier": "leaf-lit",
  "badge": "shared Lit web component",
  "element": "aha-info-box",
  "summary": "A settings-only tinted callout in four tones (information / success / warning / error), optionally dismissible — a bespoke component, not Ant Alert.",
  "lead": "ONE custom element — <aha-info-box> — the settings-panel callout. Four tones (information / success / warning / error), each with its own tinted background, border and leading glyph, and an optional dismiss control. It is a status region (role=\"status\") so assistive tech announces it. Dismiss fades the box out on a PERSISTENT node (opacity + a class toggle, never a subtree rebuild) and emits `dismiss`; the host owns any persisted \"don't show again\" state. The glyph is summoned by name via <aha-icon> — never an inline svg. Bespoke by design — NOT Ant Alert. Shadow-DOM CSS, themed only by --aha-* tokens.",
  "codeNote": "Same element, same shadow-DOM CSS → identical render. React (< 19) via a thin ref wrapper; Vue binds the custom element natively. Defined once in @ahaslides-product/design.",
  "tokensUsed": ["bg-informative", "border-info", "color-info", "bg-positive", "border-success", "color-success", "bg-warning", "border-warning", "color-warning", "bg-negative", "border-error", "color-error", "text-default", "icon-muted", "bg-hover", "radius-default", "radius-sm"],
  "spec": [
    { "label": "Box", "value": "padding 12 · radius 8 · 1px tinted border + tinted fill per tone" },
    { "label": "Information", "value": "#F4F8FF fill · #BFD2FF border · #9BB3E9 glyph (system-info)" },
    { "label": "Success", "value": "#D8FAEF fill · #16C49A border/glyph (system-check-circle)" },
    { "label": "Warning", "value": "#FFF5F0 fill · #FF7747 border/glyph (system-warning-circle)" },
    { "label": "Error", "value": "#FFF1F0 fill · #F5222D border/glyph (system-x-circle)" },
    { "label": "Dismiss", "value": "optional ✕ (system-x); fades out on a persistent node, emits `dismiss`" },
    { "label": "Text", "value": "Plus Jakarta 14/21 #1A1A1A" }
  ],
  "props": [
    { "name": "variant", "type": "information | success | warning | error", "default": "information", "desc": "The tone — sets fill, border and leading glyph" },
    { "name": "icon", "type": "string", "default": "—", "desc": "Override the tone's default glyph with a named DS icon" },
    { "name": "dismissible", "type": "boolean", "default": "false", "desc": "Show the ✕ dismiss control" },
    { "name": "dismiss", "type": "CustomEvent", "default": "—", "desc": "Composed event after the box fades out" }
  ],
  "opinion": {
    "whenToUse": [
      { "what": "Info box", "when": "a settings-panel callout explaining a state or a consequence in place" },
      { "what": "Help text", "when": "a one-line must-see consequence attached to a single setting (≤90 chars) — not a box" },
      { "what": "Tooltip", "when": "secondary what/why/how elaboration on a ? glyph" }
    ],
    "note": "Bespoke, NOT Ant Alert — Alert renders outside the DS theme. Keep callouts rare: most explanation is a ? tooltip or nothing (SETTINGS-28). Use error/warning for a consequence, not decoration.",
    "dsv3": "COVERED — the settings-lab InfoBox control mapped into the DS. The four tinted tones reuse the DS V3 semantic bg/border/colour tokens; the settings-only callout shape + dismiss are the addition. Deliberately bespoke (not Ant Alert), which does not consume the DS theme."
  },
  "surfaces": ["editor", "settings"],
  "reuse": { "entry": "./aha-info-box", "registers": "aha-info-box", "exportsNamed": ["AhaInfoBox", "defineAhaInfoBox"] },
  "snippets": [
    { "key": "html", "label": "HTML", "file": "info-box.html.txt", "runnable": "paste-and-run", "note": "The native form — a web component in an HTML file renders on open, no build step." },
    { "key": "react", "label": "React", "file": "info-box.react.txt" },
    { "key": "vue", "label": "Vue 3", "file": "info-box.vue.txt" }
  ],
  "preview": "info-box.preview.html",
  "playground": {
    "controls": [
      { "prop": "variant", "label": "Tone", "default": "information", "options": [
        { "label": "Information", "value": "information" },
        { "label": "Success", "value": "success" },
        { "label": "Warning", "value": "warning" },
        { "label": "Error", "value": "error" }
      ] }
    ]
  },
  "selfCheck": [
    { "label": "four tones, each tinted fill + border + glyph on-palette", "verdict": "PASS" },
    { "label": "role=status; glyph by name via <aha-icon> (no inline svg)", "verdict": "PASS" },
    { "label": "dismiss fades out on a persistent node (transition fires), emits dismiss", "verdict": "PASS" },
    { "label": "bespoke — not Ant Alert", "verdict": "PASS" }
  ],
  "conformance": {
    "$about": "Measured by qa.mjs against the RENDERED UI (getComputedStyle via headless CDP). Leaf = ONE shared element, single render.",
    "ready": "!!(document.querySelector('aha-info-box[data-probe]') && document.querySelector('aha-info-box[data-probe]').shadowRoot && document.querySelector('aha-info-box[data-probe]').shadowRoot.querySelector('.box'))",
    "singleUI": true,
    "measure": "(function(){var el=document.querySelector('aha-info-box[data-probe]');var r=el&&el.shadowRoot;var b=r&&r.querySelector('.box');var t=r&&r.querySelector('.text');var bs=b?getComputedStyle(b):null;var ts=t?getComputedStyle(t):null;return{bg:bs?bs.backgroundColor:null,border:bs?bs.borderTopColor:null,radius:bs?bs.borderTopLeftRadius:null,fontSize:ts?ts.fontSize:null};})()",
    "expect": { "bg": "rgb(244, 248, 255)", "border": "rgb(191, 210, 255)", "radius": "8px", "fontSize": "14px" }
  }
}
